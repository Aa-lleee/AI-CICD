# VehicleIQ — Pinned dependencies
# tensorflow-cpu used here (CI / local dev); the Dockerfile base image
# already provides the full GPU-capable tensorflow==2.15.0 so pip skips it.
tensorflow-cpu==2.15.0
flask==3.0.3
scikit-learn==1.4.2
pandas==2.2.2
numpy==1.26.4
joblib==1.4.2
gunicorn==22.0.0